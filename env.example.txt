# Database Connection String
# Replace with your actual PostgreSQL connection string
DATABASE_URL="postgresql://username:password@localhost:5432/agri_site_db"

# NextAuth Configuration
# URL where your application is hosted
NEXTAUTH_URL="http://localhost:3000"
# Secret key used to encrypt session cookies - generate a strong random string
NEXTAUTH_SECRET="generate-a-strong-random-secret-key-here"

# Admin User (will be used for initial admin account setup)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="strong-initial-password"

# Email Service (for contact form)
EMAIL_SERVER_USER="your-email@example.com"
EMAIL_SERVER_PASSWORD="your-email-password"
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT=587
EMAIL_FROM="no-reply@yourdomain.com"

# Google Maps API (for embedded map on contact page)
NEXT_PUBLIC_MAPS_API_KEY="your-google-maps-api-key"

# Optional: Analytics (if implementing tracking)
# NEXT_PUBLIC_ANALYTICS_ID="your-analytics-id"

# Optional: Content Delivery Network
# NEXT_PUBLIC_CDN_URL="https://your-cdn-url.com"

# Production flag
NODE_ENV="development"
